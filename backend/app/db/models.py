# SQLAlchemy models for the Approved Sources Registry and everything built on top of it.
#
# This file is intentionally empty as project scaffolding. The 9 tables (sources, chunks,
# users, queries, drafts, reviews, communication_memory, terminology_guide, audit_log) are
# fully specified, field by field, in specs/001-data-model-registry/spec.md.
#
# Implemented in branch feature/001-data-model-registry — that is Task 1 on TASKS.md,
# and every other task depends on it merging first. Do not add tables here on any other
# branch; propose a schema change as a PR against the spec file first.
